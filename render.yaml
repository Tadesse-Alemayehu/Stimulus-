databases:
  - name: stimulus
    databaseName: portfolio_28me
    user: tadesse_alemayehu

services:
  - type: web
    name: stimulus
    env: ruby
    buildCommand: "./bin/render-build.sh"
    startCommand: "bundle exec puma -C config/puma.rb"
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: mysite
          property: connectionString
      - key: RAILS_MASTER_KEY
        sync: false
